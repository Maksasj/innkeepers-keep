using UnityEngine;
using Unity.Netcode;

public class ClientRigedBody : Rigidbody
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
